<h1>Editing Admin</h1>

<%= render 'form', admin: @admin %>

<br>

<%= link_to 'Back', admins_path, class: 'btn btn-secondary' %>
<%= link_to 'Change My Password', edit_user_registration_path, class: 'btn btn-danger' %>
