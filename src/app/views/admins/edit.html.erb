
<%= render 'form', admin: @admin %>

<br>


<% if current_user.admin.id == @admin.id %>
  <%#= link_to 'Back', admins_path, class: 'btn btn-secondary' %>
  <%= link_to 'Change My Password', edit_user_registration_path, class: 'btn btn-outline-danger' %>
<% else %>
  <%#= link_to 'Back', admins_path, class: 'btn btn-secondary' %>
<% end %>



